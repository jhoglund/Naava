- if @object.booker.default_session
  Vi har mottagit din kursanmälan för #{t("misc.day_names.#{@object.booker.default_session.starts_at.strftime("%A").downcase}", :count => 2)} klockan #{strip_tags(l(@object.booker.default_session.starts_at, :format => :time))} till #{strip_tags(l(@object.booker.default_session.ends_at, :format => :time))}
  
  Första kurstillfället är #{strip_tags(l(@object.booker.starts_at, :format => :long))}
- if !@object.booker.free? and !@object.paid?
  Du når din anmälan på #{booking_url(@object)} där du enkelt kan betala för kursen med kreditkort. Det går även bra att betala till vårt bankgiro 5091-8051. Märk inbetalningen "Ref.nr #{@object.payment.id}"
- else
  Du når din anmälan på #{booking_url(@object)}.
= strip_tags(render(:partial => '/sessions/house_rules')).gsub!(/^\s+(\S)/,'- \1')
Om du har några frågor så maila oss gärna på #{AppConfig[:support_mail]} eller ring oss på #{AppConfig[:support_phone]}.

Välkommen,
Jonas Höglund
