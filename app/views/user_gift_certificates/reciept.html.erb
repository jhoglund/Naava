<%= render :partial => '/user_gift_certificates/gift_certificate', :object => @payment.item %>

<%- unless @payment.item.paid? -%>
  <p>
    Att betala: <%= number_to_currency(@payment.item.gift_certificate.value) %>    
  </p>
  <p>
  Du kan betala för våra presentkort snabbt och säkert via <%= link_to_function 'PayPal', 'forms[0].submit()' %>. Har du inget PayPal konto
  kan enkelt betala med ett kreditkort.
  </p>
  <%= render :partial => "/paypal/button", :locals => { 
    :item_id => @payment.to_param, 
    :notify_url => url_for(:host => PaypalConfig[:host], :controller => 'user_gift_certificates', :action => 'paypal_ipn'), 
    :return_url => url_for(:host => PaypalConfig[:host], :controller => 'user_gift_certificates', :action => 'paypal_success', :id => @payment.to_param),
    :cancel_url => url_for(:host => PaypalConfig[:host], :controller => 'user_gift_certificates', :action => 'paypal_cancel', :id => @payment.to_param),
    :ammount => @payment.item.gift_certificate.value, 
    :item_name =>  @payment.item.gift_certificate.name
  }   %>  
  <p>
  Det går även bra att betala till vårt bankgiro <strong>5091-8051</strong>. Märk inbetalningen <strong>"<%= @payment.item.gift_certificate.name %> (ref.nr <%= @payment.item.id %>)"</strong>
  </p>
<%- else -%>
  <%- if @payment.item.payment.type? :paypal -%>
  <p>
   Presentkortet är betalt och ditt PayPal referencenummer för betalningen är <strong><%= @payment.reciept.transaction_id %></strong>
  </p>
  <%- end -%>
<%- end -%>
