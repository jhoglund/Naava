.columns.columns-twentyfour
  .column
    .content
      %h2= @instructor.name
.columns.columns-eighteen-six
  .column
    .content
      = @instructor.bio.gsub(/\n/,'<br>')
      %p
        Du når #{h @instructor.first_name} bäst på
        %strong= mail_to @instructor.email
        eller på telefon
        = succeed "." do
          %strong= @instructor.phone
  .column
    .content
      = image_tag @instructor.avatar.url(:medium)
