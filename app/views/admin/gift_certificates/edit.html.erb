<h1>Editing presentkort</h1>

<% form_for(@gift_certificate, :url => admin_gift_certificate_path(@gift_certificate)) do |f| %>
  <%= render :partial => "form", :object => f  %>
  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= link_to 'Show', admin_gift_certificate_path(@gift_certificate) %> |
<%= link_to 'Back', admin_gift_certificates_path %>