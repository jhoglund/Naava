<h1>New course</h1>

<% form_for(@course, :url => admin_course_types_path) do |f| %>
  <%= render :partial => 'form', :object => f  %>
  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', admin_course_types_path %>