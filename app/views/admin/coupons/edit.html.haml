%h1 Editing coupon
- form_for(@coupon, :url => admin_coupon_path(@coupon) ) do |f|
  = render :partial => "form", :object => f
  %p
    = f.submit 'Update'
= link_to 'Show', admin_coupon_path(@coupon)
|
\#{link_to 'Destroy', admin_coupon_path(@coupon), :method => :delete} |
\#{link_to 'Back', admin_coupons_path}
