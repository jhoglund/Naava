<h1>Editing presentkort</h1>

<% form_for(@coupon_type, :url => admin_coupon_type_path(@coupon_type)) do |f| %>
  <%= render :partial => "form", :object => f  %>
  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= link_to 'Show', admin_coupon_type_path(@coupon_type) %> |
<%= link_to 'Back', admin_coupon_types_path %>