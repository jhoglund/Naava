<h1>New instructor</h1>

<% form_for(@instructor, :url => admin_instructors_path, :html => { :multipart => true }) do |f| %>
  <%= f.error_messages %>
  <%= render :partial => 'form', :object => f %>
  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', admin_instructors_path %>